spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: establishment-microservice
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:establishmentdb}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:12345}
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
    # hibernate:
    #   ddl-auto: update

server:
  port: ${PORT:8082}
  shutdown: graceful

eureka:
  instance:
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 30
    lease-expiration-duration-in-seconds: 90
  client:
    service-url:
      defaultZone: http://${EUREKA_HOSTNAME:localhost}:${EUREKA_PORT:8761}/eureka/

swagger:
  api-gateway-url: ${API_GATEWAY_URL:http://localhost:8080/establishment}